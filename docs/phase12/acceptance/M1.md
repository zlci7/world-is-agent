# Phase12 M1 验收记录

日期：2026-09-26

## 交付入口

```powershell
cd D:\data\project\game-agent\world-is-agent\console\web
npm ci
npm run build
cd ..\..
go run ./runtime/cmd/server
```

Runtime 启动后，终端输出本地工作台地址。工作台首次进入时完成真实 Provider 连接验证。

## M1 能力

- 本地浏览器工作台直接进入故事，不依赖旧 Adapter 或游戏进程。
- 旧渡口客栈场景包含沈岚、铁杉两名重要 NPC 和 10 名有身份的旁观者。
- 私聊内容按接收者分发；未被告知的角色只能获得可见但听不清的感知。
- NPC 的反应并行生成，公共回复按角色顺序稳定呈现。
- 未明确指向人物的普通进入、环顾和走动默认不触发所有 NPC 发言；观察、等待和维持姿态不会作为需要协调结果的新行动。
- 回合先解析玩家意图、直接对象和可见范围；合法的模型语义结果不会被全文关键词再次覆盖。
- NPC 的 speech、action_intent、silent、memory 均进入各自边界；协调阶段把每个行动尝试解析为有来源的结果，人物按实际可见范围获得结果感知。
- 协调和玩家正文使用独立调用；最终正文只接收玩家可见对白与结果投影，不接收 NPC 私人记忆或隐藏结果。
- 玩家行动以明确来源进入正文投影；默认正文使用“你”的第二人称有限视角，不把主角改写为“来人”或“来客”，也不使用主角无法确认的全知判断。
- 正文允许补充临时、低影响的氛围、陈设、动作衔接和瞬时身体反应；不能新增 NPC 决策、替玩家作关键选择或改变下一轮持续状态，不逐个点名无关在场人物。
- 故事设置按存档保存正文人称、篇幅、描写密度和补充写作偏好；另存继承设置，运行中的回合不会被中途改写。
- 私聊进入正文调用前会脱敏为交谈事实，耳语原文不会通过玩家行动事件泄漏给正文 Agent。
- 第二阶段保留每名 NPC 自己在第一阶段获得的感知与个人决定，同时接收新的公开刺激。
- 只有当前在场人物参与本轮；回合结束后的场景与在场名单随事件、感知和正文在同一事务中提交，进入或离开不会回填此前阶段的信息。
- 输入运行支持完成、取消、失败、重试和请求幂等；失败与取消不会进入正式对话历史。
- 重试绑定稳定 input_id、attempt、输入接受顺序和原世界基线；任何后续新输入一旦接受，更早输入即失去重试资格，旧存档中没有可信序列的失败项不会被直接重放。
- 工作台默认进入首页；当前世界与页面导航彼此独立，返回首页不取消生成或推进时间。首页可继续当前故事、选择剧本、读取或删除存档。
- 世界支持自动保存、另存为新世界、激活切换、删除和重启恢复；当前及非当前存档均可经确认删除，活动运行或另存期间会阻止删除。
- 每个世界同时只允许一个待另存任务；另存只在绑定回合完成边界发布，复制任务在退出前取消并排空，复制状态与世界状态使用同一应用事务。
- 模型连接验证通过后使用版本化密钥和原子配置发布；发布失败时上一组配置与密钥保持可启动状态。
- 页面刷新可从服务端恢复进行中和失败回合；空运行列表稳定返回数组，用户清空失败输入后轮询不会重新填回；本地 API 校验 Host 与 Origin。
- 每个世界独立保存事件、感知、记忆、消息、角色状态和运行状态。

## 工程验证

```text
go test ./... -count=1
go test -race ./runtime/internal/storyapp ./runtime/internal/storyapi -count=1
go vet ./runtime/internal/storyapp ./runtime/internal/storyapi ./runtime/cmd/server
npm run type-check
npm run build
git diff --check
```

专项覆盖：

| 能力 | 验证 |
| --- | --- |
| 私聊隐私边界与原子提交 | storyapp 单测、storyapi 单测 |
| 结构化目标解析、协调/叙述投影隔离、在场过滤与场景人物同步 | storyapp 专项单测 |
| 正文人称、篇幅、描写密度、玩家事件来源、私聊脱敏与另存继承 | storyapp/storyapi 单测与前端类型检查 |
| NPC 行动结果、接收者感知和阶段内个人上下文 | storyapp 专项单测 |
| 旁观者感知与稳定公共顺序 | storyapp 单测、storyapi 单测 |
| 取消、失败、输入顺序重试、旧存档零基线和幂等 | storyapp 单测 |
| 自动保存、失败边界、另存发布与隔离读取 | storyapp/storyapi 单测 |
| 首页/游玩导航解耦、当前世界删除和活动版本推进 | storyapp/storyapi 单测与前端类型检查 |
| 无目标 NPC 默认沉默、被动动作归一化与正文自由边界 | storyapp 提示词合同和归一化单测 |
| 单一另存预约、复制边界、退出排空与重启恢复 | storyapp 单测 |
| 重复 JSON 键、空值字段、空结构化响应、未知字段、权限会话与 Origin | storyapp/storyapi 单测 |
| 空运行列表、运行状态查询、失败输入恢复和当前在场人物 UI | storyapp/storyapi 单测与前端类型检查 |
| 同数据根进程互斥、模型快照和配置/密钥原子发布 | storyapp 单测与实现检查 |
| Race、静态检查、前端类型与构建 | 命令验证 |
| 可运行二进制和本地 HTTP 会话 | 启动探测 |

## M1 范围边界

M1 已覆盖产品要求中的普通游玩、两名重要 NPC、旁观者、私聊隐私、可靠运行、自动保存、另存为和世界切换主链路。真实 Provider 的正式对话验收需要运行环境提供 DeepSeek 或 OpenAI API Key；Fake Generator 仅用于自动化测试。M1 不包含后续阶段的完整世界剧情、长期记忆工作台、桌面安装包和服务器部署。

## 本地交付提交

- `d4351d9 feat: add narrative story runtime for phase12`
- `5862924 fix: harden phase12 story persistence`
- `b5965e0 feat: add phase12 player console`
- `3da2b6d feat: add phase12 runtime launcher`
- `acc5f61 fix: preserve npc addressee and speaker attribution`
- `6f3f11a fix: close phase12 M1 orchestration and recovery gaps`
- `b07a458 fix: separate story coordination from player projection`
- `9ddc034 fix: bind retries to accepted input order`
- `702821e fix: harden save and model publication lifecycle`
- `cab4f93 fix: stabilize run recovery and scene roster UI`
- `88ca71f fix: restore home navigation and world deletion`
- `0c80c9e fix: bound npc reactions and narrative freedom`
