{
  "turn_timeout_ms": 90000,
  "llm_timeout_ms": 8000,
  "observe_timeout_ms": 3000,
  "action_timeout_ms": 3000,
  "action_start_timeout_ms": 3000,
  "async_action_timeout_ms": 45000,
  "max_steps": 3,
  "max_tool_calls_per_step": 4,
  "max_tool_calls_per_turn": 6,
  "max_async_actions_per_turn": 1,
  "max_parallel_tool_calls": 4,
  "memory_store": {
    "kind": "sqlite",
    "root": "runtime/.local/memory",
    "busy_timeout_ms": 5000,
    "max_records_per_entity": 100,
    "max_projection_batches_per_entity": 400
  },
  "max_tool_result_output_tokens": 8192,
  "max_tool_result_output_depth": 4,
  "max_tool_result_output_fields": 64,
  "max_tool_result_output_array_items": 32,
  "prompt": {
    "language": "Simplified Chinese",
    "npc_style": "自然、简短、符合当前角色与世界状态的语气",
    "max_speak_chars": 60,
    "tool_instruction": "Use available tools only when the NPC should take an environment action. Choose tools from their descriptions and input schemas. If a tool result reports rejected, failed, invalid, cancelled, or interrupted, use the next step to adjust or settle. Use settle when no more environment action is needed."
  }
}
